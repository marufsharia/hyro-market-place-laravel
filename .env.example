# Hyro Marketplace Environment Configuration
# 
# PRODUCTION CONFIGURATION CHECKLIST:
# 1. Set APP_ENV=production
# 2. Set APP_DEBUG=false
# 3. Generate new APP_KEY with: php artisan key:generate
# 4. Configure database connection (MySQL/PostgreSQL recommended)
# 5. Set SESSION_DRIVER=redis and CACHE_STORE=redis
# 6. Set QUEUE_CONNECTION=redis
# 7. Set FILESYSTEM_DISK=s3 and configure AWS credentials
# 8. Set SESSION_SECURE_COOKIE=true (requires HTTPS)
# 9. Configure MAIL settings for email verification
# 10. Set proper SESSION_DOMAIN for your domain
# 11. Configure trusted proxy settings if behind load balancer
# 12. Set LOG_CHANNEL=daily or stack for production logging
#
# See documentation for detailed production deployment guide.

APP_NAME="Hyro Marketplace"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration
# Local development uses SQLite
DB_CONNECTION=sqlite

# Production database configuration (MySQL/PostgreSQL)
# Uncomment and configure for production:
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=hyro_marketplace
# DB_USERNAME=root
# DB_PASSWORD=
# DB_POOL_MIN=2
# DB_POOL_MAX=10

# Session & Cache Configuration
# Local: Use 'database' or 'file'
# Production: Use 'redis' for better performance and scalability
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=false
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
# Production: Use 'redis' for queue processing
QUEUE_CONNECTION=database

# Cache Configuration
# Local: Use 'file' or 'database'
# Production: Use 'redis' for optimal performance
CACHE_STORE=database
CACHE_PREFIX=hyro_marketplace

# Redis Configuration
# Required for production caching, sessions, and queue processing
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_DB=0
REDIS_CACHE_DB=1
REDIS_PREFIX=hyro_marketplace

# Mail Configuration
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="noreply@hyro.test"
MAIL_FROM_NAME="${APP_NAME}"

# AWS S3 Configuration
# Production: Use S3 for file storage (plugin logos, etc.)
# Set FILESYSTEM_DISK=s3 in production
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false
AWS_ENDPOINT=
AWS_URL=

# CDN Configuration (optional)
# Use CDN for serving static assets in production
CDN_URL=

# Admin Configuration
ADMIN_EMAIL=admin@hyro.test

# Security Configuration
# Trusted proxies (use '*' to trust all proxies, or comma-separated IPs)
TRUSTED_PROXIES=
# CORS allowed origins (comma-separated URLs, or '*' for all)
CORS_ALLOWED_ORIGINS=*
# Enable Content Security Policy (recommended for production)
CSP_ENABLED=false

VITE_APP_NAME="${APP_NAME}"
